# 任务阶段报告模板

> 使用说明：每完成或阻塞一个任务，复制本模板到 `memory_bank/reports/<TASK-ID>.md`。不得删除必填章节；无内容时明确填写“无”。

## 基本信息

| 字段 | 内容 |
| --- | --- |
| 任务 ID | `<TASK-ID>` |
| 任务名称 | `<任务名称>` |
| 任务优先级 | `P0 / P1 / P2` |
| 最终状态 | `DONE / BLOCKED` |
| 执行日期 | `YYYY-MM-DD` |
| 对应验收标准 | `<AC 编号或任务验收标准>` |

## 1. 本阶段完成的任务

- 目标：`<本任务要解决的问题>`
- 完成情况：`<完成了什么；若阻塞，说明完成到哪里>`
- 范围偏差：`<与原任务相比的偏差及原因；无则填写“无”>`

## 2. 修改了哪些文件

| 文件 | 变更类型 | 变更说明 |
| --- | --- | --- |
| `<path/to/file>` | `新增 / 修改 / 删除` | `<一句话说明>` |

## 3. 实现了哪些关键逻辑

- `<关键逻辑 1：输入、处理和输出>`
- `<关键逻辑 2：状态、异常或边界条件>`
- `<关键逻辑 3：与现有模块的集成方式>`

### Sub-agent 使用情况

- 委派内容：`<子任务和文件边界；未使用则说明原因>`
- 主代理审查：`<发现的问题、集成调整和审查结论>`

## 4. 验证命令的结果

| 命令 | 退出码 | 结果 | 关键输出或证据 |
| --- | ---: | --- | --- |
| `<npm run ...>` | `<0/非0>` | `PASS / FAIL / BLOCKED` | `<摘要或日志路径>` |

总体结论：`<所有门禁是否通过；未通过项及原因>`

## 5. 测试案例的结果

| 测试案例 | 类型 | 结果 | 证据或备注 |
| --- | --- | --- | --- |
| `<案例名称>` | `单元 / 组件 / E2E / 人工` | `PASS / FAIL / BLOCKED` | `<测试名、截图或录屏路径>` |

覆盖的 PRD 验收项：`<AC-01、AC-02...；无则说明>`

## 6. 是否有遗留问题

- 已知问题：`<问题、影响范围和复现方式；无则填写“无”>`
- 已接受风险：`<风险、接受人或依据；无则填写“无”>`
- 阻塞项：`<所需输入和解除条件；无则填写“无”>`

## 7. 下一步建议

- 下一任务：`<TASK-ID 与名称>`
- 前置检查：`<开始下一任务前需要确认的事项>`
- 建议动作：`<具体且可执行的下一步>`

## 状态更新确认

- [ ] 已更新 `memory_bank/task-list.md` 总览状态。
- [ ] 已更新对应任务详情中的 `TODO 状态`。
- [ ] 总览与详情状态一致。
- [ ] 报告中的命令和测试结果可复核。

